# 🖼️ Image Normalization & Pixel Scale

## 📏 Pixel Scale মানে কী?

**Pixel scale** মানে হলো —  
একটা image-এর pixel value গুলোর **range বা scale** কেমন তা।

| Image Type        | Pixel Range  | Scale Type          |
|--------------------|--------------|----------------------|
| 8-bit grayscale    | 0–255        | integer scale        |
| 16-bit image       | 0–65,535     | high dynamic range   |
| Normalized image   | 0–1          | float scale          |
| Centered image     | –1 থেকে +1    | symmetric scale      |

> সব image এক scale-এ না-ও থাকতে পারে।  
> তাই machine learning বা image processing-এর আগে pixel value গুলোকে একই scale-এ আনা হয় —  
> যাকে বলে **Normalization**।

---

## 🧠 Image Normalization কী?

**Image normalization** মানে হচ্ছে কোনো ছবির (image-এর) pixel value গুলোকে একটা নির্দিষ্ট range-এ আনা।  
Normalization করা হয় যাতে সব image-এর pixel value একই রকম scale-এ আসে।

### উদাহরণ:
ধরা যাক একটা image-এর pixel value `50` থেকে `200` পর্যন্ত আছে,  
অন্য image-এর value `0` থেকে `255`।  
এই দুইটা image যদি একসাথে machine learning model-এ দেই,  
তাহলে model confused হতে পারে।  

👉 তাই আমরা normalization করি,  
যাতে সব image-এর value হয় এক রেঞ্জে — যেমন **0 থেকে 1**।

---

## 🔢 Min–Max Normalization কেন করা হয়?

**Min–Max Normalization Formula:**

$$
x' = \frac{x - x_{min}}{x_{max} - x_{min}}
$$


$$
x = x' \times 255
$$

অর্থাৎ, normalized image-কে 255 দিয়ে গুণ করলে  
তা আবার 0 থেকে 255 range-এর মধ্যে চলে আসে।


**যেখানে:**
- \( x \) = original pixel value  
- \( x_{min} \), \( x_{max} \) = image-এর সর্বনিম্ন ও সর্বোচ্চ pixel value  
- \( x' \) = normalized pixel value (যেটা থাকবে 0 থেকে 1 এর মধ্যে)

### কেন করা হয়:
- 📉 সব pixel value একই scale-এ আনার জন্য (যেমন 0–1 বা –1 থেকে +1)।  
- 🤖 Machine learning model বা neural network-এ input দিলে training দ্রুত ও স্থিতিশীল হয়।  
- 💡 বিভিন্ন ছবির brightness/contrast ভিন্ন হলেও তুলনাযোগ্য করা যায়।  

---

## ⚖️ Normalization করলে কি Image-এর ক্ষতি হয়?

সাধারণভাবে ❌ **না, কোনো ক্ষতি হয় না।**

Normalization করলে শুধু pixel value-র **scale পরিবর্তন হয়**,  
কিন্তু ছবির **relative difference (contrast, pattern, texture)** একই থাকে।

> 🎯 অর্থাৎ, normalization image-এর আসল তথ্য নষ্ট করে না,  
> বরং dataset-কে model-friendly করে তোলে।

---

## ✅ সংক্ষেপে

| বিষয় | ব্যাখ্যা |
|-------|-----------|
| Pixel scale কি ভিন্ন হতে পারে? | ✅ হ্যাঁ, 0–255, 0–1, এমনকি –1 থেকে +1 পর্যন্ত হতে পারে। |
| Normalization করলে ক্ষতি হয়? | ❌ না, শুধু scale পরিবর্তন হয়, image-এর আসল তথ্য একই থাকে। |

---


